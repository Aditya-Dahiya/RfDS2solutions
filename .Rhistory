select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
unnest_longer(address_components) |>
unnest_wider(address_components, names_sep = "_") |>
unnest_wider(address_components_types, names_sep = "_")
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
unnest_longer(address_components) |>
unnest_wider(address_components) |>
unnest_wider(address_components_types, names_sep = "_")
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
unnest_longer(address_components) |>
unnest_wider(address_components) |>
unnest_wider(types, names_sep = "_")
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
unnest_longer(address_components) |>
unnest_wider(address_components) |>
unnest_wider(types, names_sep = "_") |>
distinct(type_2)
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
unnest_longer(address_components) |>
unnest_wider(address_components) |>
unnest_wider(types, names_sep = "_") |>
distinct(types_2)
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
unnest_longer(address_components) |>
unnest_wider(address_components) |>
unnest_wider(types, names_sep = "_") |>
distinct(types_1)
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
unnest_longer(address_components) |>
unnest_wider(address_components) |>
unnest_wider(types, names_sep = "_") |>
distinct(types_1) |>
as_vector() |>
unname() |>
str_flatten()
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
unnest_longer(address_components) |>
unnest_wider(address_components) |>
unnest_wider(types, names_sep = "_") |>
distinct(types_1) |>
as_vector() |>
unname() |>
str_flatten_comma()
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
unnest_longer(address_components) |>
unnest_wider(address_components) |>
unnest_wider(types, names_sep = "_") |>
distinct(types_1) |>
as_vector() |>
unname() |>
str_flatten_comma() |>
janitor::make_clean_names()
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
unnest_longer(address_components) |>
unnest_wider(address_components) |>
unnest_wider(types, names_sep = "_") |>
distinct(types_1) |>
as_vector() |>
unname() |>
str_flatten_comma() |>
janitor::make_clean_names(case = "title")
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
unnest_longer(address_components) |>
unnest_wider(address_components) |>
unnest_wider(types, names_sep = "_") |>
distinct(types_1) |>
as_vector() |>
unname() |>
janitor::make_clean_names(case = "title") |>
str_flatten_comma()
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components)
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
unnest_longer(address_components)
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
# unnest_longer(address_components) |>
unnest_wider(address_components)
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
# unnest_longer(address_components) |>
unnest_wider(address_components, names_sep = "_")
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
unnest_longer(address_components)
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results) |>
select(address_components) |>
unnest_longer(address_components) |>
unnest_wider(address_components, names_sep = "_")
gmaps_cities |>
unnest_wider(json) |>
select(-status)
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results)
gmaps_cities |>
unnest_wider(json) |>
select(-status) |>
unnest_longer(results) |>
unnest_wider(results)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
unnest_longer(address_components)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
unnest_longer(address_components) |>
unnest_wider(address_components, names_sep = "_")
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
unnest_wider(address_components, names_sep = "_")
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
unnest_longer(address_components, names_sep = "_")
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
unnest_longer(address_components)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
unnest_longer(address_components) |>
unnest_wider(address_components)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
unnest_longer(address_components) |>
unnest_wider(address_components) |>
unnest_wider(types)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
unnest_longer(address_components) |>
unnest_wider(address_components) |>
unnest_wider(types, names_sep = "_")
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
unnest_longer(address_components) |>
unnest_wider(address_components)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
unnest_longer(address_components)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
unnest_longer(address_components) |>
print(n = Inf)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
unnest_longer(address_components) |>
print(n = Inf)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
unnest_longer(address_components) |>
unnest_wider(address_components) |>
unnest_wider(types, names_sep = "_")
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
unnest_longer(address_components)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
# unnest_longer(address_components) |>
unnest_wider(address_components)
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
# unnest_longer(address_components) |>
unnest_wider(address_components, names_sep = "_")
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
# unnest_longer(address_components) |>
unnest_wider(address_components, names_sep = "_") |>
pivot_longer(names_to = "component_level",
values_to = "address_components")
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
# unnest_longer(address_components) |>
unnest_wider(address_components, names_sep = "_") |>
pivot_longer(cols = -formatted_address,
names_to = "component_level",
values_to = "address_components")
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
# unnest_longer(address_components) |>
unnest_wider(address_components, names_sep = "_") |>
pivot_longer(cols = -formatted_address,
names_to = "component_level",
values_to = "address_components") |>
mutate(component_level = parse_number(component_level))
gmaps_cities |>
unnest_wider(json) |>
# Remove status as it adds no info
select(-status) |>
# Unnamed lists, so unnest longer - make rows
unnest_longer(results) |>
# Named lists to unnest wider into columns
unnest_wider(results) |>
# Select an id variable and address_components
select(formatted_address, address_components) |>
# Since we know the address components have City, County, State and Country
# Names, lets try to create a column for each by unnest_wider
# unnest_longer(address_components) |>
unnest_wider(address_components, names_sep = "_") |>
pivot_longer(cols = -formatted_address,
names_to = "component_level",
values_to = "address_components") |>
mutate(component_level = parse_number(component_level)) |>
unnest_wider(address_components)
